coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      target: auto
      if_no_uploads: error

    patch:
      target: "80%"
      if_no_uploads: error

    changes: false

comment:
  layout: "header, diff, changes, suggestions"
  behavior: default
